version: '3.8'

services:
  markdown-explorer:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./Markdowns:/app/Markdowns
    restart: unless-stopped
    environment:
      - NODE_ENV=production
